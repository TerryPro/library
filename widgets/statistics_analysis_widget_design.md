# 时序数据集统计分析组件设计方案

## 📊 概述

基于现有的 `DynamicTrendWidget` 架构，本文档设计了一个专注于统计性图表的交互式分析组件 `StatisticsAnalysisWidget`，用于对时序数据集的每一列进行基础统计分析，并通过直观的图形化方式展示统计结果。

## 🎯 设计目标

- **基础统计分析**: 提供核心的描述性统计和分布分析能力
- **精简可视化**: 聚焦5种最常用的统计图表类型
- **简洁交互**: 类似 `DynamicTrendWidget` 的交互式参数选择，配置简洁明了
- **高性能处理**: 支持大数据集的基础统计计算和可视化
- **模块化设计**: 基于现有架构，易于理解和维护

## 🏗️ 整体架构设计

### 组件结构

```
statistics_analysis_widget/
├── main.py                 # 主控制器 StatisticsAnalysisWidget
├── ui_components.py        # UI组件管理器 StatisticsUIComponents
├── chart_renderer.py       # 统计图表渲染器 StatisticsChartRenderer
├── data_processor.py       # 数据处理和统计计算 StatisticsDataProcessor
├── constants.py           # 常量定义
└── utils.py              # 工具函数
```

### 核心类设计

#### 1. StatisticsAnalysisWidget (主控制器)

**继承关系**: `ipywidgets.VBox`

**主要职责**:
- 整体组件协调和状态管理
- 用户交互事件处理和分发
- 组件生命周期管理

**核心属性**:
```python
class StatisticsAnalysisWidget(VBox):
    def __init__(self, df: pd.DataFrame, columns: Optional[List[str]] = None,
                 chart_type: str = 'summary', title: str = '统计分析',
                 figsize: Tuple[int, int] = (16, 10)):

        self.df = df.copy()
        self.title = title
        self.columns = columns or []
        self.chart_type = chart_type
        self.figsize = figsize

        # 状态管理
        self.state = {
            'selected_columns': [],
            'chart_type': chart_type,
            'statistics_config': {},
            'time_window': None,
            'group_by': None
        }

        # 初始化组件
        self.data_processor = StatisticsDataProcessor(self.df)
        self.ui_components = StatisticsUIComponents(self.df)
        self.chart_renderer = StatisticsChartRenderer(self.figsize)
```

#### 2. StatisticsUIComponents (UI组件管理器)

**主要职责**:
- 创建和管理所有用户界面组件
- 处理用户交互事件
- 提供参数选择和配置界面

**核心组件**:
- 列选择器 (支持多选和数据类型过滤)
- 图表类型选择器 (下拉菜单)
- 统计指标配置面板
- 时间窗口选择器
- 工具栏 (重置、导出、设置等)

#### 3. StatisticsChartRenderer (图表渲染器)

**继承关系**: 基于 `plotly.graph_objects.FigureWidget`

**主要职责**:
- 根据选择的图表类型渲染统计图表
- 处理图表布局和样式配置
- 管理图表的交互功能

**支持的图表类型**:
- 描述性统计图表
- 分布分析图表
- 时间序列统计图表
- 多变量对比图表
- 异常值分析图表

#### 4. StatisticsDataProcessor (数据处理器)

**主要职责**:
- 执行统计计算和数据预处理
- 管理统计计算的结果缓存
- 处理异常值检测和数据分组

**核心方法**:
```python
class StatisticsDataProcessor:
    def calculate_basic_statistics(self, columns: List[str]) -> pd.DataFrame:
        """计算基础统计量"""

    def calculate_distribution_stats(self, columns: List[str]) -> Dict:
        """计算分布特征统计"""

    def detect_outliers(self, data: pd.Series, method: str = 'iqr') -> pd.Series:
        """异常值检测"""

    def calculate_time_series_stats(self, data: pd.Series) -> Dict:
        """时间序列统计计算"""
```

## 📈 支持的统计图表类型

### 1. 统计汇总表 (Summary Table)
- 显示各列的主要统计指标表格
- 支持排序和导出功能
- 实时更新显示

### 2. 箱线图 (Box Plot)
- 显示数据的四分位数分布
- 清晰标记异常值
- 支持多列对比显示

### 3. 直方图 (Histogram)
- 数据分布频率可视化
- 支持自定义分组数量
- 可叠加正态分布曲线

### 4. 密度图 (Density Plot)
- 数据的概率密度函数估计
- 适合连续数据的分布特征分析
- 支持多列叠加对比

### 5. 散点图 (Scatter Plot)
- 用于两个变量的相关性分析
- 支持回归线显示
- 适用于相关性探索

## 🔧 核心功能特性

### 1. 交互式参数选择

#### 列选择器
- 支持多选数值列进行分析
- 自动过滤非数值列
- 一键全选/取消全选

#### 图表类型选择器
- 5种核心统计图表类型选择
- 清晰的图表类型说明
- 快速切换不同图表视图

#### 基础配置选项
- 图表尺寸调整
- 颜色主题选择
- 导出功能

### 2. 智能数据处理

#### 自动数据验证
- 检测数据类型和质量
- 自动处理缺失值
- 异常值基础检测

#### 统计计算优化
- 基础统计量快速计算
- 结果缓存避免重复计算
- 支持大数据集采样处理

## 📊 统计指标体系

### 基础统计量
- **集中趋势**: 均值、中位数、众数
- **离散程度**: 标准差、最小值、最大值、四分位距
- **分布位置**: 四分位数 (Q1, Q3)

### 分布特征
- **偏度 (Skewness)**: 分布的对称性度量
- **峰度 (Kurtosis)**: 分布的尖峰程度

### 数据质量指标
- **完整性**: 样本数量、缺失值数量
- **异常值**: 基于IQR方法的异常值检测

## 🔄 数据处理流程

### 1. 数据预处理
- 自动检测数值列
- 基础的缺失值处理（删除或填充）
- 简单的数据类型转换

### 2. 统计计算
- 批量计算各列的基础统计量
- 缓存计算结果提高性能
- 支持异常值基础检测

### 3. 图表渲染
- 根据选择的图表类型渲染对应的plotly图表
- 统一的布局和样式配置
- 响应式图表显示

## 🎨 用户界面设计

### 主界面布局

```
┌─────────────────────────────────────┐
│  [标题] 统计分析                    │
├─────────────────┬───────────────────┤
│ 控制面板        │ 图表显示区         │
│ ┌─────────────┐ │                   │
│ │ □ 列选择器  │ │                   │
│ │   └ 多选框   │ │                   │
│ │             │ │                   │
│ │ □ 图表类型   │ │                   │
│ │   └ 下拉菜单 │ │                   │
│ │             │ │                   │
│ │ □ 配置选项   │ │                   │
│ │   └ 简单设置 │ │                   │
│ └─────────────┘ │                   │
│                 │                   │
│ 工具栏          │                   │
│ [重置][导出]    │                   │
└─────────────────┴───────────────────┘
```

### 交互设计原则

#### 1. 简洁直观
- 最少的配置选项，快速开始
- 清晰的参数选择流程
- 即时的图表更新

#### 2. 用户友好
- 自动检测合适的列
- 友好的错误提示
- 基本的导出功能

## 🛠️ 技术实现要点

### 1. 模块化设计

#### 核心类结构
```
StatisticsAnalysisWidget
├── StatisticsUIComponents    # UI组件管理
├── StatisticsDataProcessor   # 数据处理和统计计算
└── StatisticsChartRenderer   # 图表渲染
```

#### 基础接口
- 数据处理接口：统计计算和数据验证
- 图表渲染接口：根据类型渲染对应的图表
- UI组件接口：参数选择和事件处理

### 2. 基础性能优化

#### 计算缓存
- 对统计结果进行缓存，避免重复计算
- 当数据或参数变化时更新缓存

#### 数据采样
- 对大数据集进行随机采样，保证响应性能
- 保持数据的代表性

### 3. 错误处理
- 基础的数据验证和错误提示
- 用户友好的错误信息显示
- 异常情况的降级处理

## 📋 实施计划

### Phase 1: 核心功能实现 (2周)

#### Week 1: 基础框架搭建
1. 创建组件目录和基础类结构
2. 实现数据处理器和基础统计计算
3. 搭建简单的UI框架

#### Week 2: 图表渲染实现
1. 实现5种核心统计图表渲染
2. 完成UI组件和交互逻辑
3. 基础功能测试和调试

### Phase 2: 优化和集成 (1周)

#### Week 3: 优化和集成
1. 性能优化和错误处理完善
2. 与现有系统集成
3. 文档编写和最终测试

## 🔗 集成方案

### 算法注册
```python
# library/algorithm/statistics_analysis/statistics_analysis.py

def statistics_analysis(df: pd.DataFrame, columns: list = None,
                       chart_type: str = 'summary', **kwargs) -> None:
    """
    时序数据统计分析算法

    Algorithm:
        name: 时序数据统计分析
        category: statistics_analysis
        prompt: 对 {VAR_NAME} 进行全面的统计分析并以图形化方式展示

    Parameters:
    df (pd.DataFrame): 输入数据表
        role: input
    columns (list, optional): 要分析的列，默认分析所有数值列
        label: 分析列
        widget: column-selector
        role: parameter
    chart_type (str): 图表类型 ('summary', 'boxplot', 'histogram', etc.)
        label: 图表类型
        widget: dropdown
        options: ['summary', 'boxplot', 'histogram', 'violin', 'qqplot', 'rolling_stats', 'seasonal_decomp', 'autocorr', 'radar', 'heatmap', 'parallel_coords', 'outlier_analysis']
        role: parameter
    """
    widget = StatisticsAnalysisWidget(df, columns, chart_type, **kwargs)
    display(widget)
```

### 配置文件
```python
# library/algorithm/statistics_analysis/__init__.py

from .statistics_analysis import statistics_analysis

__all__ = ['statistics_analysis']
```

### 使用示例

#### 基本使用
```python
from library.algorithm.statistics_analysis import statistics_analysis

# 基本统计分析 - 显示汇总表
statistics_analysis(df)

# 箱线图分析
statistics_analysis(df, chart_type='boxplot')

# 指定列进行分析
statistics_analysis(df, columns=['temperature', 'pressure'], chart_type='histogram')
```

#### 完整参数使用
```python
# 指定列和图表类型
statistics_analysis(df,
                   columns=['temp', 'pressure', 'humidity'],
                   chart_type='histogram',
                   figsize=(12, 8))
```

## 📚 总结

这个精简版的 `StatisticsAnalysisWidget` 设计方案：

1. **架构一致性**: 基于现有的 `DynamicTrendWidget` 架构，保持代码风格统一
2. **功能聚焦**: 专注5种最常用的统计图表，满足核心统计分析需求
3. **简洁易用**: 简化参数配置，降低使用门槛，提高用户体验
4. **性能适中**: 基础的缓存和采样优化，保证常用场景下的响应性能
5. **易于维护**: 模块化设计，代码结构清晰，易于理解和维护

该组件专注于统计性图表的可视化，为时序数据分析提供直观、有用的统计洞察。
